// src/pages/_app.js or pages/_app.js (if you don't have a src directory)
import '../app/globals.css';

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
